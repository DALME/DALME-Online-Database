{
    "title": "Date",
    "data": "date",
    "name": "date",
    "defaultContent": "",
    "visible": true,
    "orderable": true,
    "searchable": true,
    "render": "function (data, type, row, meta) {return data == \"\" ? moment(row.accessDate).format(\"YYYY\") : moment(data).format(\"YYYY\");}",
    "className": "nowrap"
}

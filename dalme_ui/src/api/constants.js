export const defaultOrder = { column: 0, dir: "asc" };
// NOTE: Clear 'magic number' violation but it saves a complexity headache
// on that backend that we can't afford to spend time on right now.
export const worksetId = 4;
